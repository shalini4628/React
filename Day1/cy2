import 'React' from 'react'

function Add()
{
    directions=direction.map(direction => direction);
    document.write(directions);
    <div>
        <button onClick={;}></button>
    </div>
  }
  export default Add;
